# Production Shopify store domain. Do not include a protocol.
PUBLIC_STORE_DOMAIN="nenufar-regalos-personalizados-xyrqi3rj.myshopify.com"

# Set by `shopify hydrogen link` or your Oxygen environment.
PUBLIC_STOREFRONT_API_TOKEN=""
PUBLIC_STOREFRONT_ID=""
# Optional: set only when the production checkout domain differs from the store domain.
PUBLIC_CHECKOUT_DOMAIN=""
PUBLIC_GTM_CONTAINER_ID="GTM-KZQS4HLW"

# Generate a unique value for each environment; do not commit the real secret.
SESSION_SECRET="replace-with-a-long-random-secret"
